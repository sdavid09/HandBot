let ranks = require('../../conf/config.json');

class Money {
    constructor(value=0) {
        this.value = 0;
    }

    getUserMoney() {
        // return user money
    }
}

module.exports = {
	Money
};